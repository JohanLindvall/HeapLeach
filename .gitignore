bin/
downloads/
node_modules/
*.tsbuildinfo

# Built UI: regenerated by `make frontend`; the placeholder is tracked.
backend/internal/webui/dist/*
!backend/internal/webui/dist/index.html

# Live tests hit the real sites. They are kept out of the repository because
# they name specific pages that go stale, and they are only useful with a
# working network. `make test-live` runs whatever is present locally.
*live_test.go

# Personal working notes, not part of the project.
CLAUDE.md

# Release archives from `make dist`.
/dist/
